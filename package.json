{
  "name": "pt-tutor",
  "version": "0.1.0",
  "description": "European Portuguese tutor with persistent learner state",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "psql $DATABASE_URL -f db/schema.sql && psql $DATABASE_URL -f db/seed.sql"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
